// import { createClient } from '@supabase/supabase-js';

// const supabase = createClient(
//   import.meta.env.VITE_SUPABASE_URL,
//   import.meta.env.VITE_SUPABASE_ANON_KEY,
//   {
//     auth: {
//       // 'implicit' flow: Supabase puts tokens in the URL hash (#access_token=...)
//       // This is the correct flow for magic-link and email-confirm redirects.
//       // If you switch to 'pkce' in Supabase dashboard, change this to 'pkce' as well.
//       flowType: 'implicit',
//       // Automatically persist and restore sessions from localStorage
//       persistSession: true,
//       detectSessionInUrl: true, // parse #access_token= from the URL on page load
//     },
//   }
// );

// export default supabase;
